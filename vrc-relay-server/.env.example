# .envに常に必要なのはAPP_PORTのみ。
# それ以外(DB接続・秘密鍵・管理者アカウント・公開URL等)は初回起動後にブラウザで
# http://<host>:<APP_PORT>/setup を開いて設定する(保存すると自動でこのファイルに追記される)。

# --- Webサーバーのポート ---
# ホスティング環境(Pterodactyl等)ごとに割り当てが変わるため固定しない。
APP_PORT=8000
